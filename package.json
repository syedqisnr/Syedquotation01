{
  "name": "optimal-command-v12",
  "version": "12.0.0",
  "description": "Optimal Command Enterprise Suite v12.0 for Optimal Surgicals & Medi Corporation",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/index.html",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
