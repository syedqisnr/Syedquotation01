{
  "name": "optimal-intelligence-v23",
  "version": "23.0.0",
  "description": "Optimal Intelligence v23 Advanced Command Center with 125+ business tools",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/index.html",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
